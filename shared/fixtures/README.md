# Shared Fixtures

This directory is for sanitized fixtures used in development and testing.

## Current Fixtures

- `export-example.json` — 完整的单房源导出示例（含 2 条看房记录、8 条看房检查项、3 张照片、1 个评分卡、1 个签约会话、4 条签约检查项、2 条签约材料），严格符合 `manifest.v1.json` schema

Do not commit real user records or real property photos here.

